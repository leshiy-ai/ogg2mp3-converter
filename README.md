# Leshiy Media Converter

Универсальный медиа-конвертер, размещённый на [Render](https://render.com).  
Преобразует аудио, видео и изображения в форматы, удобные для TTS, Whisper, аватаров и Telegram-ботов.

🚀 **Работает на бесплатном тарифе Render**  
🔌 **Поддерживает OGG, PCM, MP4, JPG/PNG**  
⚡ **Быстро, без GPU, на базе FFmpeg**

---

## 🔗 [URL сервиса](https://leshiy-mp3-converter.onrender.com)

---

## 📥 Эндпоинты

# 1. Проверка работоспособности
GET /
Leshiy Media Converter is ready!

# 2. Версия FFmpeg
GET /debug
→ Возвращает полную информацию о FFmpeg (включая --enable-libopus).

# 3. Голосовые → MP3
POST /ogg2mp3
Content-Type: multipart/form-data
Поле: audio (файл)
Формат входа: audio/ogg (OPUS, как из Telegram)
Выход: audio/mpeg (MP3, 22050 Гц, моно, 64 кбит/с)
✅ Используется для обработки голосовых сообщений Telegram.

# 4. Сырой PCM → MP3
POST /pcm2mp3?sampleRate=24000&channels=1&format=s16le
Content-Type: application/octet-stream
Тело запроса: сырые аудиобайты (например, от Gemini/ElevenLabs TTS)
Параметры (опционально):
sampleRate: 8000, 16000, 22050, 24000, 44100, 48000 (по умолчанию: 24000)
channels: 1 или 2 (по умолчанию: 1)
format: s16le, s16be, f32le (по умолчанию: s16le)
Выход: audio/mpeg (MP3)

# 5. Видео → MP3 (извлечение аудио)
POST /video2mp3
Content-Type: multipart/form-data
Поле: video (файл, например, video/mp4)
Выход: audio/mpeg (MP3, 22050 Гц, 128 кбит/с)
Проверка: если аудиодорожка отсутствует → ошибка

# 6. Поворот изображения
POST /rotate-image?angle=90
Content-Type: multipart/form-data
Поле: image (файл: JPG, PNG и др.)
Параметры:
angle: 90, -90, 180
Выход: повернутое изображение в исходном формате
💡 Используется в Telegram-ботах для интерактивного поворота фото.

# 7. Поворот видео
POST /rotate-video?angle=90
Content-Type: multipart/form-data
Поле: video (файл: MP4 и др.)
Параметры:
angle: 90, -90, 180
Выход: video/mp4 (аудио копируется без перекодировки)
⚠️ Рекомендуется для видео до 1–2 минут (ограничение Render Free Tier).

# 8. Стоп-кадр из видео (видео → изображение)
POST /video2image?timestamp=00:00:05&format=jpg
Content-Type: multipart/form-data
Поле: video (файл: MP4, MOV и др.)
Параметры:
timestamp — момент времени в формате HH:MM:SS.mmm (по умолчанию: 00:00:01.000)
format — jpg или png (по умолчанию: jpg)
Выход: JSON-объект с полями:
image: строка вида image/jpeg;base64,...
width: ширина изображения в пикселях (число)
height: высота изображения в пикселях (число)
format: формат (jpg / png)
success: true
Использование: генерация превью, анализ кадров, подготовка к Image-to-Image (I2I)
💡 Поддерживает любые форматы видео, понятные FFmpeg.
⚠️ Рекомендуется для видео до 2 минут (ограничение Render Free Tier).

# 9. Создание GIF или видео-стикера из видео
POST /video2gif?start=2&end=5&format=gif&width=320
Content-Type: multipart/form-data
Поле: video (файл: MP4 и др.)
Параметры:
start — начало фрагмента (в секундах, например 2)
end — конец фрагмента (в секундах, например 5)
format — gif или mp4 (для видео-стикеров в Telegram)
width — ширина (по умолчанию: 480)
fps — кадров в секунду (по умолчанию: 10)
Выход:
image/gif — если format=gif
video/mp4 — если format=mp4 (без звука, оптимизировано под Telegram)
💡 Идеально для создания анимаций, мемов и стикеров.
⚠️ Длительность фрагмента ≤ 5 сек (ограничение Render Free Tier).

# 10. Преобразование GIF → MP4 (видео без звука)
POST /gif2video
Content-Type: multipart/form-data
Поле: gif (анимированный GIF)
Выход: video/mp4 (оптимизированное под Telegram, H.264, YUV420p)
Использование: уменьшение размера, совместимость с AI-видео-сервисами
💡 Автоматически корректирует разрешение до чётных значений (требование H.264).

# 11. Изменение разрешения видео
POST /resize-video?resolution=720p
Content-Type: multipart/form-data
Поле: video (файл: MP4 и др.)
Параметры:
resolution — 240p, 360p, 480p, 580p, 720p, 1080p (по умолчанию: 480p)
Выход: video/mp4 в указанном разрешении (пропорции сохранены, аудио копируется без перекодировки)
💡 Идеально для подготовки видео под Telegram, AI-генерацию или экономии трафика.
⚠️ Рекомендуется для видео до 2 минут (ограничение Render Free Tier).

## 🛠 Технические детали
Среда: node:18 (Ubuntu, через Docker)
FFmpeg: 5.1.8 из официального репозитория Debian (apt install ffmpeg)
Порт: автоматически process.env.PORT (Render использует 10000 по умолчанию)
Временные файлы: хранятся в /tmp, удаляются после обработки
Безопасность: имена файлов генерируются случайно, нет path traversal
⚠️ Ограничения (Render Free Tier)

# Ресурс  Лимит
RAM    512 МБ
CPU    0.1–0.25 vCPU
Таймаут запроса  ~50 секунд
Размер файла    рекомендуется ≤ 50 МБ

## Пример использования (curl)
# Поворот изображения:
bash
> curl -X POST "https://leshiy-mp3-converter.onrender.com/rotate-image?angle=90" \
>  -F "image=@photo.jpg" \
>  -o rotated.jpg

# PCM → MP3:
bash
> curl -X POST "https://leshiy-mp3-converter.onrender.com/pcm2mp3?sampleRate=24000" \
>  -H "Content-Type: application/octet-stream" \
>  --data-binary @speech.pcm \
>  -o output.mp3
  
## 📦 Зависимости
express
multer
ffmpeg (установлен в Dockerfile)
