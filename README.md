# ⚡ Leshiy Media Converter

Универсальный медиа-конвертер, размещённый на [Render](https://render.com).  

🚀 **Работает на бесплатном тарифе Render**  
🔌 **Поддерживает OGG, PCM, MP4, JPG/PNG**  
⚡ **Быстро, без GPU, на базе FFmpeg**

> 💡 Используется для преобразования аудио, видео и изображений в форматы, удобные для TTS, Whisper, аватаров и интеграции с AI-моделями.

---

## ✨ Ключевые особенности

| Характеристика | Описание |
| :--- | :--- |
| **Основа** | **FFmpeg** — самый мощный и надёжный инструмент для медиа. |
| **Размещение** | Работает на бесплатном тарифе [Render](https://render.com) (Node.js/Docker). |
| **Поддержка** | Поддерживает ключевые форматы: **OGG** (Telegram), **PCM** (TTS), **MP4, JPG/PNG**. |
| **Скорость** | Высокая производительность без необходимости в GPU. |

---

## 🔗 [URL сервиса](https://leshiy-media-converter.onrender.com)

---

## 🛠 Эндпоинты (API)

Все POST-запросы, если не указано иное, ожидают `Content-Type: multipart/form-data`.

### 1. Отладка и Информация

| Эндпоинт | Метод | Описание | Выход |
| :--- | :--- | :--- | :--- |
| `/` | `GET` | Проверка работоспособности. | `text/plain` (`Leshiy Media Converter is ready!`) |
| `/debug` | `GET` | Версия и конфигурация FFmpeg (включая `--enable-libopus`). | `text/plain` |

### 2. Аудио-конвертация

| Эндпоинт | Описание | Вход | Выход | Использование |
| :--- | :--- | :--- | :--- | :--- |
| `/ogg2mp3` | **Голосовые → MP3** | `audio/ogg` (OPUS, из Telegram) | `audio/mpeg` (MP3, 22050 Гц, моно, 64 кбит/с) | Обработка голосовых сообщений для последующей транскрипции (Whisper). |
| `/pcm2mp3` | **Сырой PCM → MP3** | `application/octet-stream` (сырые байты) | `audio/mpeg` (MP3) | Конвертация вывода TTS-сервисов (Gemini, ElevenLabs). **Параметры:** `sampleRate`, `channels`, `format`. |
| `/video2mp3` | **Видео → MP3** | `video/mp4` (файл) | `audio/mpeg` (MP3, 22050 Гц, 128 кбит/с) | Извлечение аудиодорожки из видеофайла. |

### 3. Обработка Изображений и Видео

| Эндпоинт | Описание | Вход | Выход | Примечание |
| :--- | :--- | :--- | :--- | :--- |
| `/rotate-image` | **Поворот изображения** | `image` (файл) | Изображение в исходном формате | **Параметр:** `angle` (90, -90, 180). |
| `/rotate-video` | **Поворот видео** | `video` (файл) | `video/mp4` | **Параметр:** `angle` (90, -90, 180). Аудио копируется. *⚠️ Рекомендуется для видео до 1–2 мин.* |
| `/resize-video` | **Изменение разрешения** | `video` (файл) | `video/mp4` | **Параметр:** `resolution` (240p до 1080p). *Пропорции сохраняются.* |
| `/video2image` | **Стоп-кадр** | `video` (файл) | `JSON` (`image: base64`, `width`, `height`) | **Параметры:** `timestamp` (HH:MM:SS.mmm), `format` (jpg/png). |
| `/video2gif` | **Видео → GIF/MP4 Стикер** | `video` (файл) | `image/gif` или `video/mp4` | **Параметры:** `start`, `end` (секунды), `format`, `width`, `fps`. *⚠️ Фрагмент ≤ 5 сек.* |
| `/gif2video` | **GIF → MP4** | `gif` (анимированный GIF) | `video/mp4` (без звука) | Используется для оптимизации и уменьшения размера, автоматическая коррекция разрешения. |

---

## 🛠 Технические детали

* **Среда исполнения:** Node.js 18 (на базе Ubuntu в Docker).
* **FFmpeg:** Версия 5.1.8 (устанавливается через `apt install ffmpeg`).
* **Хостинг:** Render Free Tier.

### ⚠️ Ограничения Render Free Tier

Пожалуйста, учитывайте ограничения бесплатного тарифа Render при использовании длительной обработки медиафайлов:

| Ресурс | Лимит | Примечание |
| :--- | :--- | :--- |
| **RAM** | 512 МБ | Ограничивает одновременную обработку больших файлов. |
| **CPU** | 0.1–0.25 vCPU | Длительная обработка может быть медленной. |
| **Таймаут запроса** | ~50 секунд | Запросы, превышающие это время, будут прерваны. |
| **Рекомендуемый размер файла** | ≤ 50 МБ | Оптимально для быстрой обработки. |

### 🔍 Пример использования (curl)

Для демонстрации работы с API:

```bash
# 1. Поворот изображения на 90 градусов:
curl -X POST "[https://leshiy-media-converter.onrender.com/rotate-image?angle=90](https://leshiy-media-converter.onrender.com/rotate-image?angle=90)" \
 -F "image=@photo.jpg" \
 -o rotated.jpg

# 2. Конвертация сырого PCM (24 кГц) в MP3:
curl -X POST "[https://leshiy-media-converter.onrender.com/pcm2mp3?sampleRate=24000](https://leshiy-media-converter.onrender.com/pcm2mp3?sampleRate=24000)" \
 -H "Content-Type: application/octet-stream" \
 --data-binary @speech.pcm \
 -o output.mp3
